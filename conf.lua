function love.conf(t)
    t.title = "Langton's Ant"
    t.version = "11.4"
    
    -- window settings
    t.window.resizable = false
    t.window.width = 900            -- game window width (in pixels)
    t.window.height = 600           -- game window height (in pixels)
    
    t.identity = "langtons_ant"
end